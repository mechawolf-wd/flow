export function mountStyling(component) {
    if (!component?.style?.sheet) return

    const style = document.createElement('style')

    style.innerText = `${component.style.sheet}`

    document.body.appendChild(style)
}