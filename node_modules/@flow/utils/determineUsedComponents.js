export const determineUsedComponents = (template) => {
    const regex = /<([A-Z]\w+)(\s+[^>]*)?>/g;
    const matches = [...template.matchAll(regex)];
    const componentNames = matches.map(match => match[1]);

    return componentNames.map(componentName => componentName.toUpperCase());
}