export function replaceInterpolationMarkers(template) {
    return template.replace(/{{\s*(.+?)\s*}}/g, '<Paint>$1</Paint>');
}

export function replaceSelfClosingTags(template) {
    const regex = /<([a-zA-Z][a-zA-Z0-9]*)(\s+[^>]*)?\/>/g;
    return template.replace(regex, '<$1$2></$1>');
}