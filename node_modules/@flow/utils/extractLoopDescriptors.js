export function extractLoopDescriptors(input) {
    const regex = /(\w+)\s+of\s+(.+)/;
    const match = input.match(regex);

    if (match) {
        return {
            itemName: match[1],
            arrayExpression: match[2].trim(),
        };
    } else {
        return null;
    }
}